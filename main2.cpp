#include <iostream>
#include <math.h>

using namespace std;

int main() {
    float x,y;
    cout << "Lab2-2" << endl;
    cout << "Enter x: ";
    cin >> x;

    if(x >= 0 )
    {
        y = sqrt(pow(x,3) + 5):
    }
    if(x>-3 && x>0)
    {
        y = 3 * pow(x,4) + 9;
    }

    cout << "y = " << y;
    return 0;
}
